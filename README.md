# ML26

As part of ML from the ground up over summer 26. 
Produced after covering supervised learning theory via Cornell CS4780.

To test theory understanding produced a neural network implemented by hand with numpy, with loss gradients evaluated via backprop and verified via comparison with finite differencing.
To learn the syntax and get ready for future projects also trained a neural network via pytorch.

Moved onto RL due to time constraints but next steps would be:
- verify manual and pytorch backprop gradients agree
- implement more adaptable algorithmic approach to manual backprop